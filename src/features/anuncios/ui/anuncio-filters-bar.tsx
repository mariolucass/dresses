"use client";

// Placeholder — implementação completa na Fase de UI
export function AnuncioFiltersBar() {
  return (
    <div data-testid="anuncio-filters-bar">
      AnuncioFiltersBar — a implementar
    </div>
  );
}
