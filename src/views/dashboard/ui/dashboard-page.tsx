// A view do dashboard do boilerplate foi substituída pelas views específicas do Desapeguei.
// As novas views ficam em: src/views/ (auth, anuncios, garagem, negociacoes, perfil, landing).

export const DashboardPage = () => {
  return (
    <main className="flex min-h-screen items-center justify-center bg-background px-4">
      <p className="text-muted-foreground text-sm">
        Dashboard do boilerplate substituído pelas views do Desapeguei.
      </p>
    </main>
  );
};
