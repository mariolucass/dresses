// A view do dashboard do boilerplate foi substituída pelas views específicas do Brechó Online.
// As novas views ficam em: src/views/ (auth, anuncios, garagem, negociacoes, perfil, landing).

export const DashboardPage = () => {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p className="text-muted-foreground text-sm">
        Dashboard do boilerplate substituído pelas views do Brechó Online.
      </p>
    </main>
  );
};
