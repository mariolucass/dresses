import { Typography, Container } from '@mui/material';

export default function MyGarage() {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 4 }}>Página garagem</Typography>
    </Container>
  );
}